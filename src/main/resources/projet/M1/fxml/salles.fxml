<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<ScrollPane fitToWidth="true" fitToHeight="false"
            style="-fx-background-color: #F8FAFC; -fx-background: #F8FAFC; -fx-border-color: transparent;"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="projet.M1.ui.SallesController">

    <VBox styleClass="page-container" spacing="24">

        <!-- En-tête -->
        <HBox alignment="CENTER_LEFT" spacing="16">
            <VBox HBox.hgrow="ALWAYS" spacing="4">
                <Label text="Gestion des salles" styleClass="page-title"/>
                <Label text="Consultez les salles et leurs équipements" styleClass="page-subtitle"/>
            </VBox>
        </HBox>

        <!-- Filtres -->
        <HBox spacing="12" alignment="CENTER_LEFT">
            <TextField fx:id="fieldRecherche"
                       promptText="Rechercher une salle…"
                       prefWidth="220"
                       styleClass="login-field"/>
            <TextField fx:id="fieldCapaciteMin"
                       promptText="Capacité min."
                       prefWidth="120"
                       styleClass="login-field"/>
            <TextField fx:id="fieldEquipement"
                       promptText="Équipement…"
                       prefWidth="180"
                       styleClass="login-field"/>
        </HBox>

        <!-- Liste des salles -->
        <ProgressIndicator fx:id="loadingIndicator" prefWidth="32" prefHeight="32"
                           visible="true" managed="true"/>
        <VBox fx:id="sallesContainer" spacing="12"
              visible="false" managed="false"/>

    </VBox>

</ScrollPane>
